{
  "name": "joyfulpath-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prisma": "^5.13.0",
    "turbo": "^2.0.4"
  },
  "packageManager": "npm@10.5.0",
  "dependencies": {
    "@prisma/client": "^5.13.0"
  }
}
